# Publicação dos PDFs

Após o upload binário, conferir cada caminho listado em `PACKAGE-ASSET-MANIFEST-11SET26.txt`, abrir os documentos pelo botão da Biblioteca e validar que o GitHub Pages entrega `application/pdf` sem 404. Não alterar os nomes sem atualizar `docs/library.js` no mesmo commit.
